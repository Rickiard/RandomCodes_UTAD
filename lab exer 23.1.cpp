#include<stdio.h>
#include<stdlib.h>

void inv(char*);

int main()
{
	char letras[];
	
	printf("Introduza uma palavra: ");
	scanf("%s", &letras);
	
	inv(letras);
	
	return 1; 
}

void inv(char*)
{
	for(int i = []; i  );
	{
		for(int l = 0; l < []; i++)
		{
			letras[i] = letras[0 + i]
		}
	}
}
